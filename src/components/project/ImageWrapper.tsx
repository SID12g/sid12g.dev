"use client";

import styled from "styled-components";

const ImageWrapper = styled.div`
  position: relative;
  overflow: hidden;
  width: 100%;
  aspect-ratio: 16 / 9;
`;

export default ImageWrapper;
